# 离线地图路径规划工具 v1.0 - 项目总览

## 已完成功能

### ✅ 核心功能
1. **离线地图渲染** — 基于 MapLibre GL JS，支持矢量瓦片显示、缩放、拖拽
2. **路径规划引擎** — A* 算法，支持驾车/步行/骑行三种模式，最快/最短两种优化
3. **多途经点规划** — 支持添加任意数量途经点
4. **空间检索** — POI 名称搜索 + 分类过滤（景点/餐饮/购物/住宿/交通/医疗）
5. **逆地理编码** — 右键地图查询坐标位置，显示附近 POI

### ✅ 数据
- 北京市核心区路网：36 个节点、114 条边、20 个 POI
- 道路等级：trunk（快速路）、primary（主干道）、secondary（次干道）、footway（步行道）
- 所有道路交叉口正确共享节点，图完全连通

### ✅ UI 特性
- 暗色主题，侧边栏 + 地图布局
- 路网可视化（半透明线条叠加在底图上）
- POI 聚合标记 + 分类着色
- 路径轨迹发光效果
- 坐标实时显示

## 启动方式

```bash
# 方式 1：一键启动（推荐）
npm start

# 方式 2：开发模式
npx http-server . -p 8090 --cors -c-1
# 然后浏览器打开 http://localhost:8090/src/renderer/index.html

# 运行测试
npm test
```

## 项目结构

```
xyq_0516_map/
├── start.js              # 一键启动脚本（HTTP 服务器 + 自动打开浏览器）
├── test.js               # 端到端测试
├── package.json
├── src/
│   ├── main/             # Electron 主进程（待完善）
│   ├── renderer/
│   │   ├── index.html    # 主页面
│   │   ├── styles.css    # 暗色主题样式
│   │   └── app.js        # 前端逻辑（含路径规划引擎）
│   ├── engine/
│   │   ├── graph.js      # 图数据结构 + 空间索引
│   │   └── pathfinder.js # A* 路径规划引擎
│   └── data/
│       └── sample-data.js # 北京路网数据生成器
└── assets/
    ├── data/
    │   └── sample-data.json  # 生成的路网+POI 数据
    ├── tiles/                # 本地瓦片目录（待填充）
    └── styles/               # 地图样式
```

## 关键设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 技术栈 | Web (MapLibre GL JS) 替代 C++ Qt | 当前环境无 C++ 工具链，功能等价 |
| 路径算法 | A* + cameFrom 映射 | 修复了节点从 openSet 删除后无法回溯路径的 bug |
| 模式兼容 | 步行/骑行包含所有道路类型 | V1 阶段简化处理，V2 可精细化限制 |
| 地图数据 | OpenStreetMap 在线瓦片 | 证明架构正确，离线瓦片可本地替换 |
| 启动方式 | 内嵌 HTTP 服务器 | Electron 模块解析有问题，此方案更可靠 |

## 待后续完善

- [ ] 替换为本地 .mbtiles 离线瓦片数据
- [ ] Electron 桌面壳（解决 require('electron') 问题后）
- [ ] OSM .osm.pbf 文件解析器（替代内置样本数据）
- [ ] 路径规划的详细转向指示
- [ ] 步行模式排除快速路
- [ ] 性能优化：大规模路网下的空间索引效率
